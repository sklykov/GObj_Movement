classdef parametersSimul < handle
    % save important parameters of simulated set of pictures with moving of
    % objects
    
    %% list of parameters
    properties
        picSize; % size of picture in pixels
        numbObj; % number of fused / drawn objects
        sigma; % related to object size (Gaussian shape)
        instantV; % array of generated instantVel
        trackL; % length of a track (frames)
        
    end
    
    methods
    end
    
end

